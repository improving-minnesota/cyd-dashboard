# Custom partition table: keeps OTA (two app slots) while giving more app
# room than the stock "default" scheme (1.31MB). App slots are 1.5MB (down from
# the previous 1.75MB) to make room for a dedicated LittleFS "logos" partition
# (512KB) that holds airline logos as files - so OTA firmware stays logo-less
# and logos can be updated by rewriting files without recompiling.
#
# NVS/otadata offsets are unchanged from the stock "default" table. spiffs
# (pool temp history) and the new logos partition moved, so on adopting this table
# any existing LittleFS data is cleared and must be re-provisioned (see
# DEVELOPER.md). Once adopted, DO NOT change partition offsets/sizes again:
# LittleFS reformats on any partition-table change, and moving app0/app1 shifts
# where OTA updates land.
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x180000,
app1,     app,  ota_1,   0x190000,0x180000,
spiffs,   data, spiffs,  0x310000,0x60000,
logos,    data, spiffs,  0x370000,0x80000,
coredump, data, coredump,0x3F0000,0x10000,
